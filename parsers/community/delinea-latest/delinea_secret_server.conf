// =====================================================================
// Delinea (Thycotic) Secret Server 
// =====================================================================
{
  timezone: "UTC",

  attributes: {
    // ---- 4 mandatory pipeline defaults ----
    "metadata.version":    "1.2.1",
    "dataSource.category": "security",
    "dataSource.name":     "Secret Server",
    "dataSource.vendor":   "Delinea",

    // ---- product / provider metadata ----
    "metadata.product.vendor_name": "Delinea",
    "metadata.product.name":        "Secret Server",
    "metadata.log_provider":        "Secret Server"
    // class_uid / category_uid are per-event and set in `mappings`.
  },

  patterns: {
    // Optional RFC3164 priority "<134>" (absent in the samples).
    priOpt:   "(?:<\\d+>)?",
    // Classic-syslog timestamp "Feb 20 02:56:57" (1-2 spaces before day).
    syslogTs: "[A-Z][a-z]{2}\\s+\\d{1,2}\\s+\\d{2}:\\d{2}:\\d{2}",
    // CEF pipe-delimited header field: anything that isn't a pipe
    // (CEF `name`/`vendor` carry spaces, so the default whitespace-stop
    // capture would fail).
    notPipe:  "[^|]*",
    // Single hostname token. NOTE: a bare `$field$` before a literal space
    // does NOT default to "stop at whitespace" — it defaults to `quotable`,
    // which (when unquoted) matches everything up to the NEXT FORMAT
    // DELIMITER, i.e. up to the literal " CEF:" later in the line. On the
    // rare malformed raw record (two log lines glued together without a
    // separating newline, e.g. a stray background-worker line ahead of the
    // real CEF payload), that let device.hostname swallow the intervening
    // text (a second embedded timestamp + pod hostname) instead of stopping
    // at the real host boundary. Bind it explicitly to one token.
    hostTok:  "\\S+"
  },

  formats: [
    {
      // Frame: syslog wrapper + 7 positional CEF header fields, then the
      // whole extension into `extension`. NO halt — but there is only one
      // format, and all field slicing happens in `rewrites` below.
      id: "thycotic_cef",
      // NOTE: `$extension$` is BARE (no pattern) on purpose — the final field
      // in a format captures the remainder of the line to EOL (spaces and all).
      // Giving it an explicit `.*` pattern (`$extension=rest$`) instead caused
      // the body to NOT capture, leaving every extension-rewrite empty.
      format: "$pri=priOpt$$ts_raw=syslogTs$ $device.hostname=hostTok$ CEF:$cef_version=notPipe$\\|$cef_vendor=notPipe$\\|$cef_product=notPipe$\\|$cef_product_version=notPipe$\\|$signature_id=notPipe$\\|$name=notPipe$\\|$cef_severity=notPipe$\\|$extension$",

      // CRITICAL: every rewrite sets `outputIfNoMatch: false`. On this tenant
      // the DEFAULT is true, which — when the regex does not match — writes the
      // INPUT (`extension`) verbatim into the output field. Without this flag,
      // events missing a field (e.g. api.operation on a LOGIN, src on a VIEW)
      // get the entire raw body dumped into the target. With it, a non-match is
      // a clean no-op and the field stays unset.
      rewrites: [
        // ---- Coarse event-class hint, derived from the CEF `name` ----
        // ("SECRET - …" / "USER - LOGIN…" / "USER - EDIT|DISABLE|ENABLE").
        // Mutually exclusive; drives the class/category constants below.
        { input: "name", output: "ss_kind", match: "^(?i)secret.*$",                         replace: "api",     outputIfNoMatch: false },
        // NOTE: "login" and "logout" are both matched here (not just "login")
        // — an earlier version only matched "user - login.*", which silently
        // left USER - LOGOUT (signature_id 17) with no ss_kind at all, and
        // therefore no class_uid/category_uid either (found via raw sample).
        { input: "name", output: "ss_kind", match: "^(?i)user - log(?:in|out).*$",                  replace: "auth",    outputIfNoMatch: false },
        // "create" added (v1.2.0) — USER - CREATE previously matched nothing.
        { input: "name", output: "ss_kind", match: "^(?i)user - (?:create|edit|disable|enable).*$", replace: "account", outputIfNoMatch: false },

        // v1.2.0: group/role membership events (10k-line raw sample turned
        // these up — none matched any prior ss_kind, so they had NO
        // class_uid/category_uid, same failure mode as the LOGOUT bug).
        // Routed to their own "group" bucket -> OCSF Group Management 3006.
        { input: "name", output: "ss_kind", match: "^(?i)user - (?:addedtogroup|removedfromgroup).*$", replace: "group", outputIfNoMatch: false },
        { input: "name", output: "ss_kind", match: "^(?i)group - .*$",                                 replace: "group", outputIfNoMatch: false },
        { input: "name", output: "ss_kind", match: "^(?i)role - assignuserorgroup.*$",                 replace: "group", outputIfNoMatch: false },

        // v1.2.0: platform/domain sync jobs — system-initiated maintenance,
        // not tied to a specific account or secret. Routed to a separate
        // "sync" bucket (NOT "api") so they get API Activity 6003 without
        // the SECRET-only `resources[0].type: "Secret"` constant below,
        // which is gated on ss_kind=='api' and would otherwise misfire.
        { input: "name", output: "ss_kind", match: "^(?i)(?:platform|domain) - synchronize.*$", replace: "sync", outputIfNoMatch: false },

        // v1.2.1: internal diagnostic/maintenance log lines (signature_id
        // 500) — no user, secret, or group involved at all. Its own bucket
        // for the same reason as "sync": must not inherit the SECRET-only
        // `resources[0].type: "Secret"` constant gated on ss_kind=='api'.
        { input: "name", output: "ss_kind", match: "^(?i)system log.*$", replace: "system", outputIfNoMatch: false },

        // ---- Actor (the operator who performed the action) -----------
        // "By User: dom\user" — single token (no space) after the label.
        // The "By\\s?User:" (optional space) tolerates a vendor localization
        // fallback seen in ~4.5% of a raw sample, where "By User:" renders as
        // "ByUser:" inside a "Language Resource Not Found: ..." wrapper.
        { input: "extension", output: "actor.user.name",   match: "^.*By\\s?User: (\\S+).*$",            replace: "$1", outputIfNoMatch: false },
        // Domain half of "dom\user" (no-op when the principal has no domain, e.g. ThycoticSystem).
        { input: "extension", output: "actor.user.domain", match: "^.*By\\s?User: ([^\\\\ ]+)\\\\.*$",    replace: "$1", outputIfNoMatch: false },
        // suid = operator's internal user id.
        { input: "extension", output: "actor.user.uid",    match: "^.*\\bsuid=([^ ]+).*$",            replace: "$1", outputIfNoMatch: false },

        // ---- Target account (USER events: duser/duid) ----------------
        // Account Change: the account that was modified. Authentication:
        // the authenticating subject (== actor). Absent on SECRET events.
        { input: "extension", output: "user.name",   match: "^.*\\bduser=([^ ]+).*$",        replace: "$1", outputIfNoMatch: false },
        { input: "extension", output: "user.domain", match: "^.*\\bduser=([^\\\\ ]+)\\\\.*$", replace: "$1", outputIfNoMatch: false },
        { input: "extension", output: "user.uid",    match: "^.*\\bduid=([^ ]+).*$",         replace: "$1", outputIfNoMatch: false },

        // ---- Source / destination endpoints --------------------------
        // src = client IP (login/launch). Target Server = the host a
        // launched secret connects to (SECRET - LAUNCH only).
        { input: "extension", output: "src_endpoint.ip",       match: "^.*\\bsrc=([^ ]+).*$",          replace: "$1", outputIfNoMatch: false },
        { input: "extension", output: "dst_endpoint.hostname", match: "^.*Target Server: (\\S+).*$",   replace: "$1", outputIfNoMatch: false },

        // ---- Original event time (CEF `rt`, values contain spaces) ---
        // Bounded by the next known key or EOL. Kept as a String in
        // metadata.original_time (never written to reserved `time`).
        { input: "extension", output: "metadata.original_time",
          match: "^.*\\brt=(.+?)(?=\\s+(?:fname|fileType|fileId|cs3Label|cs3|cs4Label|cs4|suid|suser|duser|duid|src)=|$).*$",
          replace: "$1", outputIfNoMatch: false },

        // ---- SECRET-only fields (gated by requiring "Event: [Secret]"
        //      in the regex, so USER/auth events never match) ----------
        // resources[0].name = the secret's display name.
        // v1.2.1: boundary now tolerates the same "language resource not
        // found: " prefix on "Item Id:" / "Container name:" / "Details" that
        // the localization fallback also applies to these labels (not just
        // the "Event:"/"Action:" brackets) — without it, the lazy capture
        // fell through to `$` and swallowed the rest of the extension.
        { input: "extension", output: "resources[0].name",
          match: "(?i)^.*event: \\[(?:language resource not found: )?secret\\].*?item name: (.+?)(?: \\((?:language resource not found: )?item id:| (?:language resource not found: )?container name:| (?:language resource not found: )?details:?|$).*$",
          replace: "$1", outputIfNoMatch: false },
        // resources[0].uid = the secret's Item Id (absent on HEARTBEAT).
        { input: "extension", output: "resources[0].uid",
          match: "(?i)^.*event: \\[(?:language resource not found: )?secret\\].*?\\(item id: ([^)]+)\\).*$",
          replace: "$1", outputIfNoMatch: false },
        // api.operation = the vendor action verb (View/Launch/Password change/…).
        { input: "extension", output: "api.operation",
          match: "(?i)^.*event: \\[(?:language resource not found: )?secret\\].*?action: \\[(?:language resource not found: )?([^\\]]+)\\].*$",
          replace: "$1", outputIfNoMatch: false },

        // ---- Remaining CEF extension k=v fields (kept vendor-native) -----
        // Values may contain single internal spaces, so each is bounded by the
        // next known key or EOL (same multi-space-safe idiom as the others).
        // cs4 = actor display name; cs4Label = its CEF label ("suser Display Name").
        { input: "extension", output: "cs4",
          match: "^.*\\bcs4=(.+?)(?=\\s+(?:suid|suser|cs4Label|cs3Label|cs3|duser|duid|fname|fileType|fileId|src|rt)=|$).*$",
          replace: "$1", outputIfNoMatch: false },
        { input: "extension", output: "cs4Label",
          match: "^.*\\bcs4Label=(.+?)(?=\\s+(?:suid|suser|cs4|cs3Label|cs3|duser|duid|fname|fileType|fileId|src|rt)=|$).*$",
          replace: "$1", outputIfNoMatch: false },
        // cs3 = folder/container name; cs3Label = its CEF label ("Folder").
        { input: "extension", output: "cs3",
          match: "^.*\\bcs3=(.+?)(?=\\s+(?:suid|suser|cs4Label|cs4|cs3Label|duser|duid|fname|fileType|fileId|src|rt)=|$).*$",
          replace: "$1", outputIfNoMatch: false },
        { input: "extension", output: "cs3Label",
          match: "^.*\\bcs3Label=(.+?)(?=\\s+(?:suid|suser|cs4Label|cs4|cs3|duser|duid|fname|fileType|fileId|src|rt)=|$).*$",
          replace: "$1", outputIfNoMatch: false },
        // fname = item full name; fileType = User|Secret; fileId = item id.
        { input: "extension", output: "fname",
          match: "^.*\\bfname=(.+?)(?=\\s+(?:suid|suser|cs4Label|cs4|cs3Label|cs3|duser|duid|fileType|fileId|src|rt)=|$).*$",
          replace: "$1", outputIfNoMatch: false },
        { input: "extension", output: "fileType", match: "^.*\\bfileType=([^ ]+).*$", replace: "$1", outputIfNoMatch: false },
        { input: "extension", output: "fileId",   match: "^.*\\bfileId=([^ ]+).*$",   replace: "$1", outputIfNoMatch: false },

        // ---- msg free-text sub-fields ----
        // Container name + id (the folder the secret/account lives in).
        { input: "extension", output: "container_name",
          match: "^.*Container name: (.+?)(?: \\(Container Id:| Details:| suid=|$).*$",
          replace: "$1", outputIfNoMatch: false },
        { input: "extension", output: "container_id",
          match: "^.*\\(Container Id: ([^)]+)\\).*$",
          replace: "$1", outputIfNoMatch: false },
        // Details free-text -> OCSF status_detail (failure reason on LOGINFAILURE,
        // change description on USER edits, RDP/connection info on LAUNCH).
        { input: "extension", output: "status_detail",
          match: "^.*Details:\\s*(.+?)(?=\\s+suid=|$).*$",
          replace: "$1", outputIfNoMatch: false }
      ]
    }
  ],

  mappings: {
    version: 1,
    mappings: [
      {
        predicate: "true",
        transformations: [
          // ---- Preserve the vendor event code ----
          { rename: { from: "name", to: "metadata.event_code" } },

          // =====================================================
          //  CLASS / CATEGORY  (coarse, by ss_kind)
          // =====================================================
          // -- API Activity (6003): SECRET credential-access --
          { constant: { value: 6003,              field: "class_uid",        predicate: "ss_kind == 'api'" } },
          { constant: { value: "API Activity",    field: "class_name",       predicate: "ss_kind == 'api'" } },
          { constant: { value: 6,                 field: "category_uid",     predicate: "ss_kind == 'api'" } },
          { constant: { value: "Application Activity", field: "category_name", predicate: "ss_kind == 'api'" } },
          { constant: { value: "Secret Server",   field: "api.service.name", predicate: "ss_kind == 'api'" } },
          { constant: { value: "Secret",          field: "resources[0].type", predicate: "ss_kind == 'api'" } },

          // -- Authentication (3002): USER LOGIN / LOGINFAILURE / LOGOUT --
          { constant: { value: 3002,           field: "class_uid",     predicate: "ss_kind == 'auth'" } },
          { constant: { value: "Authentication", field: "class_name",  predicate: "ss_kind == 'auth'" } },
          { constant: { value: 3,              field: "category_uid",  predicate: "ss_kind == 'auth'" } },
          { constant: { value: "Identity & Access Management", field: "category_name", predicate: "ss_kind == 'auth'" } },

          // -- Account Change (3001): USER CREATE / ENABLE / DISABLE / EDIT --
          { constant: { value: 3001,            field: "class_uid",    predicate: "ss_kind == 'account'" } },
          { constant: { value: "Account Change", field: "class_name",  predicate: "ss_kind == 'account'" } },
          { constant: { value: 3,               field: "category_uid", predicate: "ss_kind == 'account'" } },
          { constant: { value: "Identity & Access Management", field: "category_name", predicate: "ss_kind == 'account'" } },

          // -- Group Management (3006): group/role membership changes (v1.2.0) --
          // USER - ADDEDTOGROUP/REMOVEDFROMGROUP, GROUP - EDIT, ROLE - ASSIGNUSERORGROUP.
          // Per the OCSF mapping guide, group/role membership changes belong
          // in Group Management, not Account Change — a class this parser
          // didn't use before v1.2.0.
          { constant: { value: 3006,              field: "class_uid",    predicate: "ss_kind == 'group'" } },
          { constant: { value: "Group Management", field: "class_name",  predicate: "ss_kind == 'group'" } },
          { constant: { value: 3,                 field: "category_uid", predicate: "ss_kind == 'group'" } },
          { constant: { value: "Identity & Access Management", field: "category_name", predicate: "ss_kind == 'group'" } },

          // -- API Activity (6003) reused for PLATFORM/DOMAIN sync (v1.2.0) --
          // Deliberately a SEPARATE ss_kind ("sync", not "api") so these
          // system-maintenance events don't pick up the SECRET-only
          // `resources[0].type: "Secret"` constant above, which is gated on
          // ss_kind=='api' — these events describe no secret at all.
          { constant: { value: 6003,           field: "class_uid",        predicate: "ss_kind == 'sync'" } },
          { constant: { value: "API Activity", field: "class_name",       predicate: "ss_kind == 'sync'" } },
          { constant: { value: 6,              field: "category_uid",     predicate: "ss_kind == 'sync'" } },
          { constant: { value: "Application Activity", field: "category_name", predicate: "ss_kind == 'sync'" } },
          { constant: { value: "Secret Server", field: "api.service.name", predicate: "ss_kind == 'sync'" } },

          // -- API Activity (6003) reused for "System Log" diagnostics (v1.2.1) --
          // No secret/user/group involved, so kept separate from "api" for
          // the same reason as "sync" above.
          { constant: { value: 6003,           field: "class_uid",        predicate: "ss_kind == 'system'" } },
          { constant: { value: "API Activity", field: "class_name",       predicate: "ss_kind == 'system'" } },
          { constant: { value: 6,              field: "category_uid",     predicate: "ss_kind == 'system'" } },
          { constant: { value: "Application Activity", field: "category_name", predicate: "ss_kind == 'system'" } },
          { constant: { value: "Secret Server", field: "api.service.name", predicate: "ss_kind == 'system'" } },

          // =====================================================
          //  ACTIVITY / STATUS / TYPE_UID / SEVERITY  (per signature)
          //  type_uid = class_uid*100 + activity_id
          //  status_id: 1 Success, 2 Failure   severity_id: 1 Info, 3 Medium
          // =====================================================
          // -- 16  USER - LOGIN  -> Logon / Success --
          { constant: { value: 1,        field: "activity_id",   predicate: "signature_id == '16'" } },
          { constant: { value: "Logon",  field: "activity_name", predicate: "signature_id == '16'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '16'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '16'" } },
          { constant: { value: 300201,   field: "type_uid",      predicate: "signature_id == '16'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '16'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '16'" } },

          // -- 18  USER - LOGINFAILURE  -> Logon / Failure --
          { constant: { value: 1,        field: "activity_id",   predicate: "signature_id == '18'" } },
          { constant: { value: "Logon",  field: "activity_name", predicate: "signature_id == '18'" } },
          { constant: { value: 2,        field: "status_id",     predicate: "signature_id == '18'" } },
          { constant: { value: "Failure", field: "status",       predicate: "signature_id == '18'" } },
          { constant: { value: 300201,   field: "type_uid",      predicate: "signature_id == '18'" } },
          { constant: { value: 3,        field: "severity_id",   predicate: "signature_id == '18'" } },
          { constant: { value: "Medium", field: "severity_name", predicate: "signature_id == '18'" } },

          // -- 17  USER - LOGOUT  -> Logoff / Success --
          { constant: { value: 2,        field: "activity_id",   predicate: "signature_id == '17'" } },
          { constant: { value: "Logoff", field: "activity_name", predicate: "signature_id == '17'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '17'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '17'" } },
          { constant: { value: 300202,   field: "type_uid",      predicate: "signature_id == '17'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '17'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '17'" } },

          // -- 2  USER - DISABLE  -> Disable / Success --
          { constant: { value: 5,        field: "activity_id",   predicate: "signature_id == '2'" } },
          { constant: { value: "Disable", field: "activity_name", predicate: "signature_id == '2'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '2'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '2'" } },
          { constant: { value: 300105,   field: "type_uid",      predicate: "signature_id == '2'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '2'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '2'" } },

          // -- 3  USER - ENABLE  -> Enable / Success --
          { constant: { value: 2,        field: "activity_id",   predicate: "signature_id == '3'" } },
          { constant: { value: "Enable", field: "activity_name", predicate: "signature_id == '3'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '3'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '3'" } },
          { constant: { value: 300102,   field: "type_uid",      predicate: "signature_id == '3'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '3'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '3'" } },

          // -- 10100  USER - EDIT  -> Other ("Edit") / Success --
          { constant: { value: 99,       field: "activity_id",   predicate: "signature_id == '10100'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "signature_id == '10100'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '10100'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '10100'" } },
          { constant: { value: 300199,   field: "type_uid",      predicate: "signature_id == '10100'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '10100'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '10100'" } },

          // -- 1  USER - CREATE  -> Create / Success (v1.2.0) --
          { constant: { value: 1,        field: "activity_id",   predicate: "signature_id == '1'" } },
          { constant: { value: "Create", field: "activity_name", predicate: "signature_id == '1'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '1'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '1'" } },
          { constant: { value: 300101,   field: "type_uid",      predicate: "signature_id == '1'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '1'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '1'" } },

          // -- 10004  SECRET - VIEW  -> Read / Success --
          { constant: { value: 2,        field: "activity_id",   predicate: "signature_id == '10004'" } },
          { constant: { value: "Read",   field: "activity_name", predicate: "signature_id == '10004'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '10004'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '10004'" } },
          { constant: { value: 600302,   field: "type_uid",      predicate: "signature_id == '10004'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '10004'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '10004'" } },

          // -- 10006  SECRET - LAUNCH  -> Read / Success --
          { constant: { value: 2,        field: "activity_id",   predicate: "signature_id == '10006'" } },
          { constant: { value: "Read",   field: "activity_name", predicate: "signature_id == '10006'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '10006'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '10006'" } },
          { constant: { value: 600302,   field: "type_uid",      predicate: "signature_id == '10006'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '10006'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '10006'" } },

          // -- 10055  SECRET - PASSWORDCHANGE  -> Update / Success --
          { constant: { value: 3,        field: "activity_id",   predicate: "signature_id == '10055'" } },
          { constant: { value: "Update", field: "activity_name", predicate: "signature_id == '10055'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '10055'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '10055'" } },
          { constant: { value: 600303,   field: "type_uid",      predicate: "signature_id == '10055'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '10055'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '10055'" } },

          // -- 10155  SECRET - PASSWORDCHANGEFAILURE  -> Update / Failure --
          { constant: { value: 3,        field: "activity_id",   predicate: "signature_id == '10155'" } },
          { constant: { value: "Update", field: "activity_name", predicate: "signature_id == '10155'" } },
          { constant: { value: 2,        field: "status_id",     predicate: "signature_id == '10155'" } },
          { constant: { value: "Failure", field: "status",       predicate: "signature_id == '10155'" } },
          { constant: { value: 600303,   field: "type_uid",      predicate: "signature_id == '10155'" } },
          { constant: { value: 3,        field: "severity_id",   predicate: "signature_id == '10155'" } },
          { constant: { value: "Medium", field: "severity_name", predicate: "signature_id == '10155'" } },

          // -- 10032  SECRET - HEARTBEATSUCCESS  -> Other / Success --
          { constant: { value: 99,       field: "activity_id",   predicate: "signature_id == '10032'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "signature_id == '10032'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '10032'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '10032'" } },
          { constant: { value: 600399,   field: "type_uid",      predicate: "signature_id == '10032'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '10032'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '10032'" } },

          // -- 10007  SECRET - HEARTBEATFAILURE  -> Other / Failure --
          { constant: { value: 99,       field: "activity_id",   predicate: "signature_id == '10007'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "signature_id == '10007'" } },
          { constant: { value: 2,        field: "status_id",     predicate: "signature_id == '10007'" } },
          { constant: { value: "Failure", field: "status",       predicate: "signature_id == '10007'" } },
          { constant: { value: 600399,   field: "type_uid",      predicate: "signature_id == '10007'" } },
          { constant: { value: 3,        field: "severity_id",   predicate: "signature_id == '10007'" } },
          { constant: { value: "Medium", field: "severity_name", predicate: "signature_id == '10007'" } },

          // -- 10158  SECRET - PRECHECKIN  -> Other / Success --
          { constant: { value: 99,       field: "activity_id",   predicate: "signature_id == '10158'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "signature_id == '10158'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '10158'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '10158'" } },
          { constant: { value: 600399,   field: "type_uid",      predicate: "signature_id == '10158'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '10158'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '10158'" } },

          // -- SECRET - WEBPASSWORDFILL  -> Other / Success --
          { constant: { value: 99,       field: "activity_id",   predicate: "metadata.event_code == 'SECRET - WEBPASSWORDFILL'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "metadata.event_code == 'SECRET - WEBPASSWORDFILL'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "metadata.event_code == 'SECRET - WEBPASSWORDFILL'" } },
          { constant: { value: "Success", field: "status",       predicate: "metadata.event_code == 'SECRET - WEBPASSWORDFILL'" } },
          { constant: { value: 600399,   field: "type_uid",      predicate: "metadata.event_code == 'SECRET - WEBPASSWORDFILL'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "metadata.event_code == 'SECRET - WEBPASSWORDFILL'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "metadata.event_code == 'SECRET - WEBPASSWORDFILL'" } },

          // -- SECRET - PASSWORD_DISPLAYED  -> Read / Success --
          { constant: { value: 2,        field: "activity_id",   predicate: "metadata.event_code == 'SECRET - PASSWORD_DISPLAYED'" } },
          { constant: { value: "Read",   field: "activity_name", predicate: "metadata.event_code == 'SECRET - PASSWORD_DISPLAYED'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "metadata.event_code == 'SECRET - PASSWORD_DISPLAYED'" } },
          { constant: { value: "Success", field: "status",       predicate: "metadata.event_code == 'SECRET - PASSWORD_DISPLAYED'" } },
          { constant: { value: 600302,   field: "type_uid",      predicate: "metadata.event_code == 'SECRET - PASSWORD_DISPLAYED'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "metadata.event_code == 'SECRET - PASSWORD_DISPLAYED'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "metadata.event_code == 'SECRET - PASSWORD_DISPLAYED'" } },

          // -- SECRET - EXPIRES01DAY / EXPIRES30DAYS / EXPIREDTODAY (expiry
          //    notifications)  -> Other / Success --
          // NOTE: one block per event code, not a single `||`-joined
          // predicate — compound boolean predicates aren't a documented/
          // confirmed feature of this DSL, and failures here are silent.
          { constant: { value: 99,       field: "activity_id",   predicate: "metadata.event_code == 'SECRET - EXPIRES01DAY'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "metadata.event_code == 'SECRET - EXPIRES01DAY'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "metadata.event_code == 'SECRET - EXPIRES01DAY'" } },
          { constant: { value: "Success", field: "status",       predicate: "metadata.event_code == 'SECRET - EXPIRES01DAY'" } },
          { constant: { value: 600399,   field: "type_uid",      predicate: "metadata.event_code == 'SECRET - EXPIRES01DAY'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "metadata.event_code == 'SECRET - EXPIRES01DAY'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "metadata.event_code == 'SECRET - EXPIRES01DAY'" } },

          { constant: { value: 99,       field: "activity_id",   predicate: "metadata.event_code == 'SECRET - EXPIRES30DAYS'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "metadata.event_code == 'SECRET - EXPIRES30DAYS'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "metadata.event_code == 'SECRET - EXPIRES30DAYS'" } },
          { constant: { value: "Success", field: "status",       predicate: "metadata.event_code == 'SECRET - EXPIRES30DAYS'" } },
          { constant: { value: 600399,   field: "type_uid",      predicate: "metadata.event_code == 'SECRET - EXPIRES30DAYS'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "metadata.event_code == 'SECRET - EXPIRES30DAYS'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "metadata.event_code == 'SECRET - EXPIRES30DAYS'" } },

          { constant: { value: 99,       field: "activity_id",   predicate: "metadata.event_code == 'SECRET - EXPIREDTODAY'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "metadata.event_code == 'SECRET - EXPIREDTODAY'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "metadata.event_code == 'SECRET - EXPIREDTODAY'" } },
          { constant: { value: "Success", field: "status",       predicate: "metadata.event_code == 'SECRET - EXPIREDTODAY'" } },
          { constant: { value: 600399,   field: "type_uid",      predicate: "metadata.event_code == 'SECRET - EXPIREDTODAY'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "metadata.event_code == 'SECRET - EXPIREDTODAY'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "metadata.event_code == 'SECRET - EXPIREDTODAY'" } },

          // =====================================================
          //  Group Management (3006) activity/status (v1.2.0)
          //  activity_id enum verified against schema.ocsf.io/1.6.0/
          //  classes/group_management (not in the bundled field-path doc,
          //  which lists paths but not enum values).
          // =====================================================
          // -- 5  USER - ADDEDTOGROUP  -> Add User / Success --
          { constant: { value: 3,          field: "activity_id",   predicate: "signature_id == '5'" } },
          { constant: { value: "Add User", field: "activity_name", predicate: "signature_id == '5'" } },
          { constant: { value: 1,          field: "status_id",     predicate: "signature_id == '5'" } },
          { constant: { value: "Success",  field: "status",       predicate: "signature_id == '5'" } },
          { constant: { value: 300603,     field: "type_uid",      predicate: "signature_id == '5'" } },
          { constant: { value: 1,          field: "severity_id",   predicate: "signature_id == '5'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '5'" } },

          // -- 6  USER - REMOVEDFROMGROUP  -> Remove User / Success --
          { constant: { value: 4,             field: "activity_id",   predicate: "signature_id == '6'" } },
          { constant: { value: "Remove User", field: "activity_name", predicate: "signature_id == '6'" } },
          { constant: { value: 1,             field: "status_id",     predicate: "signature_id == '6'" } },
          { constant: { value: "Success",     field: "status",       predicate: "signature_id == '6'" } },
          { constant: { value: 300604,        field: "type_uid",      predicate: "signature_id == '6'" } },
          { constant: { value: 1,             field: "severity_id",   predicate: "signature_id == '6'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '6'" } },

          // -- 10141  GROUP - EDIT  -> Other / Success --
          // (no dedicated "Update"/"Rename" value in the Group Management
          // activity_id enum; GROUP - EDIT is a group-property rename, so
          // "Other" is the closest fit, same reasoning as USER - EDIT above.)
          { constant: { value: 99,       field: "activity_id",   predicate: "signature_id == '10141'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "signature_id == '10141'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '10141'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '10141'" } },
          { constant: { value: 300699,   field: "type_uid",      predicate: "signature_id == '10141'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '10141'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '10141'" } },

          // -- 10  ROLE - ASSIGNUSERORGROUP  -> Assign Privileges / Success --
          { constant: { value: 1,                    field: "activity_id",   predicate: "signature_id == '10'" } },
          { constant: { value: "Assign Privileges",   field: "activity_name", predicate: "signature_id == '10'" } },
          { constant: { value: 1,                    field: "status_id",     predicate: "signature_id == '10'" } },
          { constant: { value: "Success",             field: "status",       predicate: "signature_id == '10'" } },
          { constant: { value: 300601,                field: "type_uid",      predicate: "signature_id == '10'" } },
          { constant: { value: 1,                    field: "severity_id",   predicate: "signature_id == '10'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '10'" } },

          // =====================================================
          //  API Activity (6003) via ss_kind=='sync' — platform/domain
          //  maintenance jobs (v1.2.0). No dedicated "sync" activity in the
          //  api_activity enum, so "Other" — same treatment as HEARTBEAT.
          // =====================================================
          // -- 10200  PLATFORM - SYNCHRONIZE  -> Other / Success --
          { constant: { value: 99,       field: "activity_id",   predicate: "signature_id == '10200'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "signature_id == '10200'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '10200'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '10200'" } },
          { constant: { value: 600399,   field: "type_uid",      predicate: "signature_id == '10200'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '10200'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '10200'" } },

          // -- 10145  DOMAIN - SYNCHRONIZE  -> Other / Success --
          { constant: { value: 99,       field: "activity_id",   predicate: "signature_id == '10145'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "signature_id == '10145'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '10145'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '10145'" } },
          { constant: { value: 600399,   field: "type_uid",      predicate: "signature_id == '10145'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '10145'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '10145'" } },

          // -- 500  System Log  -> Other / Success (v1.2.1) --
          // Internal diagnostic/maintenance log lines (SecretEraseMonitor,
          // DiscoveryConsumer, platform-sync exceptions) — same "Other"
          // treatment as HEARTBEAT/sync since there's no dedicated enum value.
          { constant: { value: 99,       field: "activity_id",   predicate: "signature_id == '500'" } },
          { constant: { value: "Other",  field: "activity_name", predicate: "signature_id == '500'" } },
          { constant: { value: 1,        field: "status_id",     predicate: "signature_id == '500'" } },
          { constant: { value: "Success", field: "status",       predicate: "signature_id == '500'" } },
          { constant: { value: 600399,   field: "type_uid",      predicate: "signature_id == '500'" } },
          { constant: { value: 1,        field: "severity_id",   predicate: "signature_id == '500'" } },
          { constant: { value: "Informational", field: "severity_name", predicate: "signature_id == '500'" } },

          // ---- User object type (POSIX/AD user) ----
          { constant: { value: 1,      field: "actor.user.type_id", predicate: "true" } },
          { constant: { value: "User", field: "actor.user.type",    predicate: "true" } }

          // NOTE: nothing is dropped. All source data is retained — the raw
          // `message`, every CEF header component (cef_*, signature_id, pri,
          // ts_raw), the bulk `extension`, the parse-routing field `ss_kind`,
          // and every key=value / free-text sub-field captured by the rewrites
          // above. Fields with a clean OCSF home are mapped there; the rest are
          // kept vendor-native and queryable.
        ]
      }
    ]
  },

  // Retain the raw event line in full.
  discardAttributes: []
}
